/**
 * Stands in for the `server-only` package while testing. That package exists to fail the build when a
 * server module is pulled into a client bundle, which is a check the test runner has no bundle to make.
 */
export {};
