/** The primary navigation. Labels are message keys, resolved against the active locale at render. */
export const NAV_ITEMS = [
  { href: '/catalog', label: 'catalog.title' },
  { href: '/playlists', label: 'playlist.title' },
] as const;
